import { describe, it } from 'vitest'
import { useAccesibilidadStore } from '../../../src/stores/'

describe('', () => {
  // creando simulación de store
  console.log(useAccesibilidadStore)
  // const store = useAccesibilidadStore()

  it('estados y acciones', () => {
    // simular que se agrega o se quita una opcion a clasesSeleccionadas
    // simular que modifican las clasesSeleccionadas
    // simular que se restablecen las clasesSeleccionadas
  })
})
